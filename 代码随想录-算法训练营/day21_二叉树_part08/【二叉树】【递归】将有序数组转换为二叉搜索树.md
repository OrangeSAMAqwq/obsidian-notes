# 题目：将有序数组转换为二叉搜索树  (Convert Sorted Array to Binary Search Tree) 🌲📐

## 题目链接  
https://leetcode.cn/problems/convert-sorted-array-to-binary-search-tree/

---

## 🧠 解题思路

### 📋 题意理解  
给定一个 **升序排序的整数数组** `nums`，需要将其转换为一棵 **高度平衡的二叉搜索树（BST）**。

要求的关键点有两个：

1. 构造出来的是 **二叉搜索树**
2. 树要 **高度平衡**（左右子树高度差不超过 1）

---

## 🌟 核心思想：分治 + 取中点

有序数组天然满足 BST 的中序遍历结果，因此：

- **数组中点作为根节点**
- 左半部分构造左子树
- 右半部分构造右子树
- 递归进行，直到区间为空

这样构造出来的树天然是 **平衡 BST**。

---

## ✨ 递归函数设计

```cpp
buildBST(nums, l, r)
```

含义：

- `nums`：有序数组（只读）
- `[l, r]`：当前子数组区间
- 返回：该区间构造出的 BST 根节点

递归规则：

- `l > r`：区间为空，返回 `nullptr`
- `mid = (l + r) / 2` 作为根节点
- `[l, mid-1]` 构造左子树
- `[mid+1, r]` 构造右子树

---

## 💻 代码实现

```cpp
class Solution {
public:
    TreeNode* buildBST(const vector<int> &nums, int l, int r){
        if (l > r)
            return nullptr;

        int mid = (l + r) / 2;
        TreeNode* root = new TreeNode(nums[mid]);

        root->left = buildBST(nums, l, mid - 1);
        root->right = buildBST(nums, mid + 1, r);

        return root;
    }

    TreeNode* sortedArrayToBST(vector<int>& nums) {
        return buildBST(nums, 0, nums.size() - 1);
    }
};
```

---

## 🔍 关键点解析

- 使用 **下标区间递归**，避免数组拷贝
- 中点作为根，保证左右子树节点数量尽量相等
- 构造顺序与 BST 性质完全一致：
  - 左 < 根 < 右
- 不依赖额外数据结构，逻辑非常干净

---

## ⏱️ 复杂度分析

设数组长度为 `n`：

| 类型 | 复杂度 |
|------|--------|
| 时间复杂度 | O(n)，每个元素只创建一次节点 |
| 空间复杂度 | O(log n)，递归栈深度（平衡树） |

---

## 🧾 总结

- 有序数组 + 取中点 = 平衡 BST
- 本题是 **BST 构造类问题中最基础、也最优雅的一题**
- 常作为：
  - 平衡 BST 的入门题
  - 分治递归思想的经典模板

```text
有序数组的中点，就是平衡 BST 的天然根节点
```

这道题和「最大二叉树」「从遍历序列构造二叉树」一起，非常适合对比记忆 🌟
