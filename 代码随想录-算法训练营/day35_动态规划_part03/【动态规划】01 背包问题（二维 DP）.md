# 题目：01 背包问题（二维 DP）🎒📦

## 题目链接  
https://kamacoder.com/problempage.php?pid=1046

---

## 🧠 解题思路

### 📋 题意理解  
有 `m` 件物品，每件物品：

- 重量 `w[i]`
- 价值 `c[i]`

背包容量为 `n`，每件物品 **只能选 0 次或 1 次**（这就是 “01” 的含义）。

目标：在不超过容量 `n` 的前提下，让总价值最大 💎

---

## 🌟 核心思想：二维动态规划（2D DP）

01 背包的经典 DP 模型：

### ✅ 状态定义

```text
dp[i][j] = 只考虑前 i 件物品，在容量为 j 的背包中，能取得的最大价值
```

- `i`：考虑到第几件物品（1..m）
- `j`：当前背包容量（1..n）

---

## ✨ 状态转移方程（选 or 不选）

对于第 `i` 件物品，有两种选择：

### 1️⃣ 不选第 i 件
```text
dp[i][j] = dp[i-1][j]
```

### 2️⃣ 选第 i 件（前提是装得下）
如果 `j >= w[i]`：
```text
dp[i][j] = dp[i-1][j-w[i]] + c[i]
```

综合取最大值：

```text
dp[i][j] = max(dp[i-1][j], dp[i-1][j-w[i]] + c[i])
```

如果 `j < w[i]`，只能不选：

```text
dp[i][j] = dp[i-1][j]
```

---

## 🔁 遍历顺序

```text
i 从 1 到 m
j 从 1 到 n
```

二维 DP 写法中，`dp[i]` 依赖 `dp[i-1]`，因此正序遍历完全 OK ✅

---

## 💻 代码实现（二维 DP）

```cpp
#include <cstdio>
#include <iostream>
#include <vector>
#include <math.h>
using namespace std;

int main(){
    int m, n;
    cin >> m >> n;
    vector<int> w(m + 1, 0);
    vector<int> c(m + 1, 0);

    for (int i = 1; i <= m; i++){
        cin >> w[i];
    }
    for (int i = 1; i <= m; i++){
        cin >> c[i];
    }

    vector<vector<int>> dp(m + 1, vector<int>(n + 1, 0));

    for (int i = 1; i <= m; i++){
        for (int j = 1; j <= n; j++){
            if (j >= w[i])
                dp[i][j] = max(dp[i - 1][j], dp[i - 1][j - w[i]] + c[i]);
            else
                dp[i][j] = dp[i - 1][j];
        }
    }

    cout << dp[m][n] << endl;
    return 0;
}
```

---

## 🔍 关键点总结

- ✅ `dp` 开到 `m+1`、`n+1`，并且默认 0：表示“不选任何物品/容量为0”的价值为 0
- ✅ 物品从 1 开始读（1-index），写状态转移更直观
- ✅ “01”的本质：状态转移用的是 `dp[i-1]`（上一行），保证每件物品最多选一次

---

## ⏱️ 复杂度分析

| 类型 | 复杂度 |
|------|--------|
| 时间复杂度 | O(m × n) |
| 空间复杂度 | O(m × n) |

---

## 🧠 可扩展思考（记录用）

二维 DP 很清晰，但可以进一步优化空间到 **一维 DP**：

- `dp[j] = max(dp[j], dp[j-w[i]] + c[i])`
- 且 `j` 必须 **从大到小遍历**（保证每件物品只用一次）

不过二维版更适合作为「01 背包模板题」📌
