# 题目：反转字符串 (Reverse String) 🔁

## 题目链接 🌐  
https://leetcode.cn/problems/reverse-string/

---

## 解题思路 🧠

### 思路分析 🧩

题目要求：  
给定一个 `vector<char>` 表示的字符数组，**原地** 将其反转（不能申请额外数组），即把整个字符序列倒过来。

最直接、最高效的方式就是使用 **双指针** 从两端向中间交换字符：

- 左指针 `l` 从 0 开始
- 右指针 `r` 从 `s.size() - 1` 开始
- 每次交换 `s[l]` 和 `s[r]`，然后：
  - `l++`
  - `r--`
- 当 `l >= r` 时，说明已经完成反转

---

## 代码实现 💻

```cpp
class Solution {
public:
    void reverseString(vector<char>& s) {
        int l = 0, r = s.size() - 1;
        while (l < r) {
            swap(s[l], s[r]);  // 交换左右字符
            l++;
            r--;
        }
        return;
    }
};
```

---

## 关键点 🔑

- **双指针**：
  - 左右对撞型双指针，适用于处理字符串/数组左右对称类问题。
- **原地修改**：
  - 只在原数组上交换元素，空间复杂度为 O(1)。
- 使用 `swap` 简化交换逻辑，代码简洁清晰。

---

## 边界情况 🚨

1. 字符串长度为 0：什么都不做。
2. 字符串长度为 1：左右指针初始即相等，不进入循环。
3. 含有空格、符号、数字等字符：一视同仁，按位置交换即可。

---

## 复杂度分析 ⏱️

- 时间复杂度：**O(n)**，其中 `n` 为字符串长度，需要遍历一半长度完成交换。
- 空间复杂度：**O(1)**，只使用了常数级变量 `l` 和 `r`。

---

## 总结 📚

这道题是 **双指针对撞法** 的经典入门题之一，思路简单但非常常用：

- 适用于数组/字符串反转
- 为之后处理回文串、区间翻转等题目打下基础。
