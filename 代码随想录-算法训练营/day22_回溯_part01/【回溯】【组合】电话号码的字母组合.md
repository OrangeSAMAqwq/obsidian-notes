# 题目：电话号码的字母组合 (Letter Combinations of a Phone Number) ☎️🔤

## 题目链接  
https://leetcode.cn/problems/letter-combinations-of-a-phone-number/

---

## 🧠 解题思路

### 📋 题意理解  
给定一个只包含 `2~9` 的字符串 `digits`，返回它能表示的所有字母组合。

手机九宫格映射如下：

- 2 → abc
- 3 → def
- 4 → ghi
- 5 → jkl
- 6 → mno
- 7 → pqrs
- 8 → tuv
- 9 → wxyz

目标：从每个数字对应的字母集合里各选一个，组成所有可能的字符串。

---

## 🌟 核心思想：回溯（Backtracking）

这是一个典型的“多层选择”问题：

- 第 1 层从 `digits[0]` 的字母里选一个
- 第 2 层从 `digits[1]` 的字母里选一个
- ...
- 当当前字符串 `t` 的长度等于 `digits.size()`，说明已经选满一组，加入答案

---

## ✨ 递归函数设计

```cpp
dfs(ans, t, hashmap, cnt, digits)
```

参数含义：

| 参数 | 含义 |
|------|------|
| `ans` | 最终答案集合 |
| `t` | 当前正在构造的字符串 |
| `hashmap` | 数字到字母串的映射表 |
| `cnt` | 当前要处理的 digits 下标 |
| `digits` | 输入数字字符串 |

---

## 🔁 回溯过程

- 从当前位置 `cnt` 开始处理 digits（你的写法用 `for (i = cnt; i < ...)`）
- 对于某一位数字 `digits[i]`：
  - 取出对应字母串 `temp`
  - 对每个字母尝试加入 `t`
  - 递归处理下一位 `i + 1`
  - 回溯撤销选择（pop）

---

## 💻 代码实现（当前写法）

```cpp
class Solution {
public:
    void dfs(vector<string> &ans, string &t, auto &hashmap, int cnt, string &digits){
        if (t.size() == digits.size()){
            ans.push_back(t);
            return;
        }
        for (int i = cnt; i < digits.size(); i++){
            string &temp = hashmap[digits[i]];
            for (int j = 0; j < temp.size(); j++){
                t.push_back(temp[j]);
                dfs(ans, t, hashmap, i + 1, digits);
                t.pop_back();
            }
        }
    }

    vector<string> letterCombinations(string digits) {
        std::unordered_map <char, string> hashmap;
        hashmap['2'] = "abc";
        hashmap['3'] = "def";
        hashmap['4'] = "ghi";
        hashmap['5'] = "jkl";
        hashmap['6'] = "mno";
        hashmap['7'] = "pqrs";
        hashmap['8'] = "tuv";
        hashmap['9'] = "wxyz";

        vector<string> ans;
        string t;
        dfs(ans, t, hashmap, 0, digits);
        return ans;
    }
};
```

---

## ⚠️ 边界情况

### 1) 输入为空串 `digits = ""`
题目要求返回空数组 `[]`。

当前写法会发生：

- `t.size() == digits.size()`（0==0）成立
- 会把空串 `""` push 进去，返回 `[""]`

因此需要在入口处加一句：

```cpp
if (digits.empty()) return {};
```

---

## ✅ 复杂度分析

设 digits 长度为 `m`：

| 类型 | 复杂度 |
|------|--------|
| 时间复杂度 | O(3^m ~ 4^m)（枚举所有组合） |
| 空间复杂度 | O(m)（递归栈 + 当前路径） |

---

## 🔧 小优化思路（更标准的写法）

这题更常见的写法是不需要外层 `for`，而是每一层只处理一个位置 `cnt`：

```cpp
void dfs(int cnt){
    if (cnt == digits.size()) ...
    for (char c : hashmap[digits[cnt]]) ...
}
```

这样结构更像“层数=digits长度”的决策树，也更直观。

---

## 🧾 总结

- 本题是回溯入门必刷题：每一层从一个字符集合中选一个
- 关键在于：
  - 映射表（数字→字母）
  - 当前处理到 digits 的哪一位
  - 选 -> 递归 -> 撤销（标准回溯模板）
- 记得处理空输入，否则会多出一个空字符串结果

```text
每一位数字就是一层选择，组合数 = 各层分支数相乘
```
